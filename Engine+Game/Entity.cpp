#include "Entity.h"



Entity::Entity()
{
	
}


/*Entity& Entity::operator=(Entity ent)
{
	std::swap(sprite, ent.sprite);
	std::swap(texture_id, ent.texture_id);
	std::swap(is_movable, ent.is_movable);
	std::swap(is_collidable, ent.is_collidable);
	std::swap(is_controllable, ent.is_controllable);
	std::swap(physicsInstance, ent.physicsInstance);

	return *this;
}*/


